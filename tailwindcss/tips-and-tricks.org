:PROPERTIES:
:ID:       49132cb8-c2f9-435f-873c-31b004ffaf7e
:END:
#+title: Top 10 Tips and Tricks with Tailwind CSS by developedbyed (yt)

* [[https://youtu.be/PL1CFZhepc0]]
* [[https://github.com/codewithjom/tips-and-tricks-tailwindcss]]
* Delegating Classess

*NumberOne.tsx*

#+begin_src typescript

  export default function NumberOne() {
    return (
      <nav>
        <ul className="text-2xl text-gray-100">
          <li className="text-violet-400">Home</li>
          <li>About</li>
          <li>Pages</li>
        </ul>
      </nav>
    );
  }

#+end_src

* Extending your styles

*tailwind.config.js* /default/

#+begin_src javascript

  /** @type {import('tailwindcss').Config} */
  module.exports = {
    content: [
      "./app/**/*.{js,ts,jsx,tsx}",
      "./pages/**/*.{js,ts,jsx,tsx}",
      "./components/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };


#+end_src

*tailwind.config.js*

#+begin_src javascript

  /** @type {import('tailwindcss').Config} */
  module.exports = {
    content: [
      "./app/**/*.{js,ts,jsx,tsx}",
      "./pages/**/*.{js,ts,jsx,tsx}",
      "./components/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
      extend: {
        gridTemplateColumns: {
          fluid: "repeat(auto-fit, minmax(15rem, 1fr))",
        },
        fontFamily: {
          montserrat: ["var(--font-montserrat)"],
        },
        colors: {
          primary: "var(--primary)",
        },
      },
    },
    plugins: [],
  };

#+end_src

*globals.css*

#+begin_src css

  :root {
    --primary: #14342b;
  }

#+end_src

*NumberTwo.tsx*

#+begin_src typescript

  export default function NumberTwo() {
    return (
      <nav>
        <ul className="text-2xl text-gray-100">
        <li className="text-primary">Home</li>
        <li>About</li>
        <li>Pages</li>
        </ul>
        </nav>
    );
  }

#+end_src

* Adding spacing

*NumberThree.tsx*

#+begin_src typescript

  export default function NumberThree() {
    return (
      <nav>
        <ul className="text-2xl text-gray-100 flex flex-col gap-2">
          <li>Home</li>
          <li>About</li>
          <li>Pages</li>
        </ul>
      </nav>
    );
  }

#+end_src

*or*:

#+begin_src typescript

  export default function NumberThree() {
    return (
      <nav>
        <ul className="text-2xl text-gray-100 space-y-12">
          <li>Home</li>
          <li>About</li>
          <li>Pages</li>
        </ul>
      </nav>
    );
  }

#+end_src

* Opacity on colors

*NumberFour.tsx*

#+begin_src typescript

  export default function NumberFour() {
    return (
      <main>
        <div className="bg-white p-12 text-center text-lg font-bold rounded-md">
          <h1>hello</h1>
        </div>
      </main>
    );
  }

#+end_src

#+begin_src typescript

  export default function NumberFour() {
    return (
      <main>
        <div className="bg-white text-teal-700 text-opacity-50 p-12 text-center text-lg font-bold rounded-md">
          <h1>hello</h1>
        </div>
      </main>
    );
  }

#+end_src

* Animations and Transitions

#+begin_src typescript

  export default function NumberFive() {
    return (
      <>
        <button className="bg-blue-500 hover:bg-blue-700 active:bg-blue-900 text-white font-bold py-2 px-4 rounded transition-colors duration-200">
          Purchase now
        </button>
      </>
    );
  }

#+end_src

* Responsive Design

*NumberSix.tsx*

#+begin_src typescript

  export default function NumberSix() {
    return (
      <nav>
        <ul className="flex text-lg gap-16">
          <li>Home</li>
          <li>About</li>
          <li>Pages</li>
          <li>Pages</li>
          <li>Pages</li>
        </ul>
      </nav>
    );
  }

#+end_src

*layout.tsx* /apply the responsive here/.

#+begin_src typescript

  import "./globals.css";

  export const metadata = {
    title: "Create Next App",
    description: "Generated by create next app",
  };

  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <html lang="en">
        <body className={`py-6 mx-6 md:mx-24`}>{children}</body>
      </html>
    );
  }

#+end_src

*or*:

#+begin_src typescript

  import "./globals.css";

  export const metadata = {
    title: "Create Next App",
    description: "Generated by create next app",
  };

  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <html lang="en">
        <body className={`py-6 max-w-lg m-auto lg:max-w-4xl`}>{children}</body>
      </html>
    );
  }

#+end_src
