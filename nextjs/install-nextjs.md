# Install NextJS

## Installation

Let's use `npm` to install nextjs.

```sh
# standard
npx create-next-app@latest project-name

# use typescript
npx create-next-app@latest --typescript project-name
```
